interface:
  display_name: "Claude Job Apply"
  short_description: "Repo-specific patterns and workflows for Claude-job-apply"
  default_prompt: "Use the Claude-job-apply repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true